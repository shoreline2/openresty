services:
  openresty:
    build: .
    ports:
      - '80:80'
    volumes:
      - './nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf'
      - './http.js:/usr/local/openresty/nginx/conf/http.js'
